'use client';

/**
 * ProfileSettings — the "Profile" tab inside the Settings page.
 * Re-exports the form from ProfileForm for the Professional tab.
 */

export { ProfileForm } from '@/components/profile/ProfileForm';